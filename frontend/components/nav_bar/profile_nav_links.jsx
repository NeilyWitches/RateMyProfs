import React from 'react';

const ProfileNavLinks = props => {
    return (
        <div id='profile-navv-links'>
            <button>{props.title}</button>
        </div>
    )
};

export default ProfileNavLinks;